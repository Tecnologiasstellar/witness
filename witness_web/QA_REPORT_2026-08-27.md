# Witness website QA report

Date: 2026-08-27  
Scope: local source at `witness_web/site`  
Deployment state: production deployed and verified

Official-domain migration state: complete; GoDaddy DNS, Vercel aliases, managed TLS, redirects, and production promotion verified

## Final production evidence

- Public URL: `https://witnessatlas.com/`
- Production deployment: `dpl_FmuXZobtMx4c6cJqY3DeqxgNsUXc`
- Immutable source preview promoted: `dpl_7EEcHfH32Vgku7wsDfeJZ1QnEiYt`
- Vercel target/status: `production` / `Ready`
- Public crawl: PASS; 39/39 routes returned HTTP 200, including `/icon.svg`.
- Production Lighthouse: Performance 95, Accessibility 100, Best Practices 100, SEO 100.
- Web vitals lab metrics: FCP 1.0s, LCP 2.5s, TBT 90ms, CLS 0, Speed Index 4.0s.
- Production visual QA: PASS at desktop and 390 x 844 mobile; no overflow, missing images, or console errors.
- Production keyboard QA: PASS for native mobile menu and FAQ toggles.
- Domain redirects: PASS; `www.witnessatlas.com` and `witness-rho.vercel.app` return HTTP 308 to the canonical apex while preserving path and query.
- Team-scoped alias: existing Vercel Deployment Protection remains active and returns the expected SSO redirect; it is not treated as a public legacy URL.
- Canonical metadata: PASS; homepage canonical and Open Graph URL resolve to `https://witnessatlas.com` and robots references the official sitemap.
- Rollback target: immediate pre-cutover production deployment `dpl_2oy1gncewf1C2Adihdq2VAP5kuPU`.

## DNS and TLS evidence

- Apex DNS: `216.198.79.1` and `64.29.17.1`, TTL 600 in GoDaddy.
- `www` DNS: `9da28e999f1f52d9.vercel-dns-017.com`, TTL 600 in GoDaddy.
- Vercel verification: PASS; both apex and `www` report `configured_correctly`, attached, and verified.
- Managed TLS: PASS; Vercel certificate issued for both domains and HTTPS returns successfully.
- Mail preservation: PASS; MX priorities 0/10, SPF, DMARC, and both DKIM CNAMEs resolve to their pre-cutover GoDaddy values.

## Vercel preview evidence

- Official-domain preview: `https://witness-qyz2kpeos-tecnologiasstellars-projects.vercel.app`
- Official-domain preview deployment: `dpl_7EEcHfH32Vgku7wsDfeJZ1QnEiYt` (`Ready`), promoted without rebuilding source.

- Preview: `https://witness-4nvnhbw96-tecnologiasstellars-projects.vercel.app`
- Deployment: `dpl_Ayhs6hCU8toWkboddv3qdBdK6Saa`
- Vercel target/status: `preview` / `Ready`
- Authenticated preview crawl: PASS; all 38 public URLs returned success.
- Deployed homepage metadata: PASS; title, canonical URL, description, Open Graph image, one H1, development-preview disclosure, and FAQ present.
- Deployed sitemap: PASS; Support and all 30 species routes present.
- Deployment Protection: existing protection remains enabled; anonymous navigation redirects to Vercel login.
- Production alias: unchanged. `witness-rho.vercel.app` still resolves to production deployment `dpl_H1WQQjnoSvV6ymSG55Y2aXJpVNmL`, created 2026-08-25.

## Automated gates

| Gate | Command or evidence | Result |
|---|---|---|
| Dependency integrity | `npm ci` | PASS; 359 packages installed, 0 vulnerabilities reported |
| Lint | `npm run lint` | PASS; exit 0, no findings |
| TypeScript | `npx tsc --noEmit` | PASS; exit 0, no findings |
| Production build | `npm run build -- --webpack` | PASS; Next.js 16.3.2 generated 41 static pages |
| Route generation | Build route manifest | PASS; homepage, icon, Method, Privacy, Support, Terms, archive index, robots, sitemap, not-found, and 30 species records |
| Local route crawl | Production server crawl | PASS; all 38 public URLs returned HTTP 200 |
| Catalog parity | Parsed-content comparison against `WitnessCore` catalog | PASS; all 30 records match exactly |
| Claim drift | Targeted `rg` scan against prohibited legacy wording | PASS for rendered strings; remaining em dashes occur only in comments |
| Secret exclusion | `.gitignore` plus `git check-ignore` | PASS; `.env.local`, `.vercel`, `.next`, and `node_modules` excluded |

The final build used Node.js 24.19.0 from the bundled workspace runtime. A damaged ignored `node_modules` install was recreated from `package-lock.json`; the native Next.js compiler then loaded and the build completed in 12.7 seconds.

## Responsive and visual QA

| View | Result |
|---|---|
| Desktop, 1440 x 1000 | PASS; no document overflow, one H1, all eight rendered images loaded, central-device composition and sticky story sequence intact |
| Mobile, 390 x 844 | PASS; no document overflow, one H1, 44px menu target, 72px FAQ summary targets, deliberate single-column composition |
| Narrow-screen CSS, 320px rules | PASS by implementation inspection; grids collapse, controls wrap, gallery remains contained, safe-area padding is present |
| Reduced motion | PASS by implementation inspection; observer-led transitions and decorative animations are disabled under `prefers-reduced-motion` |
| Dark appearance | PASS in implemented token system; all sections use semantic surface and text tokens |

The final punctuation-only and sitemap edits did not change layout geometry. A fresh automated browser session was unavailable for a second 320px capture, so that specific capture remains an optional pre-production recheck rather than a claimed browser result.

## Accessibility and interaction

- Semantic header, navigation, main, footer, headings, native details/summary FAQs, and native radio controls are present.
- Skip link and visible `:focus-visible` treatment are implemented.
- Touch targets meet the 44px minimum in the tested mobile navigation and exceed it in FAQ controls.
- Images have explicit dimensions, useful alternative text, and eager/lazy loading appropriate to placement.
- Sticky anchors use scroll offset; interactive motion is transform/opacity based and has a reduced-motion fallback.
- No autoplaying audio/video, drag-only control, hover-only disclosure, or color-only status is present.

## Links and metadata

- All internal destinations correspond to generated routes or in-page IDs.
- The Apple standard EULA, GitHub repository, catalog action destinations, and declared sources are direct HTTPS links.
- The 202 unique external destinations include GitHub, Apple's EULA, and catalog source/action links. Catalog destinations preserve their record-level `lastVerified` or `lastAccessed` evidence; this pass did not replace that provenance with a transient bulk crawler result.
- Canonical URL, description, Open Graph image, Twitter card, robots instructions, sitemap, language, and theme colors are present.
- `/support` is included in the final sitemap alongside Homepage, Archive, Method, Privacy, Terms, and all 30 records.
- The live sitemap contains 36 canonical pages; together with robots, sitemap, and the referenced icon asset, the final public crawl covered 39 endpoints with zero failures.

## Known gates outside this website build

- Final in-app screenshots are not yet supplied; the phone is explicitly labeled a development interface preview.
- App Store availability, production count integrity, purchases, final App Store privacy disclosures, and remaining release gates are not proven by this site.
- Terms are product copy, not a substitute for legal review.
- Final native app screenshots are still the next visual-content replacement gate; website deployment itself is verified.
